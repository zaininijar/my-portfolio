import { Box, VStack } from "@chakra-ui/react";
import React from "react";

type Props = {};

const HomeMobile = (props: Props) => {
  return (
    <VStack width={`100%`}>
      <Box>Hello World (Mobile)</Box>
    </VStack>
  );
};

export default HomeMobile;
